# Routes

HTTP route ownership is currently composed in `../server.js` to preserve the tested API behavior during the refactor. New endpoints should be extracted here as router modules and mounted from `server.js`.
