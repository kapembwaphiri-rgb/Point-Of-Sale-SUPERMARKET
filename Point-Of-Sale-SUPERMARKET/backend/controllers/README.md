# Controllers

Controllers should translate HTTP input into application operations. The current server keeps these handlers inline while the project is being migrated; this folder is the extraction boundary for authentication, sales, inventory, returns, and reporting controllers.
