import { Receipt } from 'lucide-react';
import { useCart } from '../context/CartContext.jsx';
import { formatZmw } from '../utils/currency.js';

export default function CartSidebar({ discount, setDiscount, formId }) { const { cart, changeQuantity, subtotal } = useCart(); const total = subtotal * (1 - discount / 100); return <aside className="cart"><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center' }}><h2>Current sale</h2><span className="muted">{cart.reduce((sum, item) => sum + item.quantity, 0)} items</span></div>{!cart.length ? <p className="muted" style={{ padding: '30px 0' }}>Your cart is ready for its first item.</p> : cart.map((product) => <div className="cart-line" key={product.id}><div><strong>{product.name}</strong><small>{formatZmw(product.price)} each</small></div><div className="qty"><button className="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 active:scale-95 transition transform duration-150 ease-in-out" onClick={() => changeQuantity(product.id, -1)} aria-label={`Remove one ${product.name}`}>-</button><span>{product.quantity}</span><button className="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 active:scale-95 transition transform duration-150 ease-in-out" onClick={() => changeQuantity(product.id, 1)} aria-label={`Add one ${product.name}`}>+</button></div></div>)}<div className="field"><label htmlFor="promotion">Promotion</label><select id="promotion" value={discount} onChange={(event) => setDiscount(Number(event.target.value))}><option value="0">No discount</option><option value="10">Welcome 10% off</option><option value="20">Manager override 20%</option></select></div><div className="total"><span>Subtotal</span><strong>{formatZmw(subtotal)}</strong></div>{discount > 0 && <p className="muted">After discount: {formatZmw(total)}</p>}<button className="bg-red-600 text-white px-4 py-2 rounded-lg hover:bg-red-700 active:scale-95 transition transform duration-150 ease-in-out primary" style={{ marginTop: 16 }} disabled={!cart.length} type="submit" form={formId}><Receipt size={16} style={{ verticalAlign: '-3px' }} /> Confirm Purchase</button></aside>; }