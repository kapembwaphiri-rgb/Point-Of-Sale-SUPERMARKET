export default function CheckoutForm({ customerName, setCustomerName, payment, setPayment, onSubmit }) {
  return <form id="checkout-form" onSubmit={onSubmit}><div className="field"><label htmlFor="customer-name">Customer Name</label><input id="customer-name" className="search" value={customerName} onChange={(event) => setCustomerName(event.target.value)} placeholder="Enter customer name" required /></div><div className="field"><label htmlFor="payment-method">Payment Method</label><select id="payment-method" value={payment} onChange={(event) => setPayment(event.target.value)}><option value="CASH">Cash</option><option value="CARD">Debit / credit card</option><option value="MOBILE_MONEY">Mobile money</option><option value="QR">QR code</option></select></div></form>;
}