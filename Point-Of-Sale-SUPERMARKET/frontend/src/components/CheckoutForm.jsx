import { Search, WifiOff } from 'lucide-react';
import ProductCard from './ProductCard.jsx';
export default function CheckoutForm({ products, search, setSearch, onAdd, offline, queued }) { return <section className="panel"><div style={{position:'relative'}}><Search size={17} style={{position:'absolute',top:13,left:14,color:'#788'}}/><input className="search" style={{paddingLeft:42}} placeholder="Search product or scan SKU..." value={search} onChange={event=>setSearch(event.target.value)} aria-label="Search products"/></div>{offline&&<div className="notice" style={{marginTop:16}}><WifiOff size={15} style={{verticalAlign:'-3px'}}/> Offline · {queued} queued</div>}<div className="product-grid">{products.map(product=><ProductCard key={product.id} product={product} onAdd={onAdd}/>)}</div></section>; }
