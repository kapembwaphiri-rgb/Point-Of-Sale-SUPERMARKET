import { BarChart3, ClipboardList, Home, LogOut, ShoppingBag, UserRound } from 'lucide-react';
import { NavLink } from 'react-router-dom';

export default function Navbar({ user, onLogout }) { return <aside className="sidebar"><h2 className="brand">freshmart<small>point of sale</small></h2><nav className="nav"><NavLink to="/" className={({ isActive }) => isActive ? 'active' : ''}><Home size={17} /> Home</NavLink><NavLink to="/catalog" className={({ isActive }) => isActive ? 'active' : ''}><ShoppingBag size={17} /> Catalog</NavLink><NavLink to="/checkout" className={({ isActive }) => isActive ? 'active' : ''}><ClipboardList size={17} /> Checkout</NavLink>{user?.role === 'MANAGER' && <NavLink to="/dashboard" className={({ isActive }) => isActive ? 'active' : ''}><BarChart3 size={17} /> Dashboard</NavLink>}</nav><div className="user-block"><strong><UserRound size={14} /> {user?.name}</strong>{user?.role === 'MANAGER' ? 'Manager' : 'Cashier'}<button className="nav" style={{ marginTop: 14, padding: 0 }} onClick={onLogout}><LogOut size={14} /> Sign out</button></div></aside>; }